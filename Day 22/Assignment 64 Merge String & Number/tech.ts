// Question No. 64
// Write a program to merge string + number

let combine = (age: string, number: number): string => {
  return age + number;
};

// showing output of the program

console.log(combine("Age: ", 28));
