// Question No. 65
// Writing a function that gets two numbers
let reminder = (number1: number, number2: number): number => {
  return number1 % number2;
};

// print the function
console.log(reminder(5, 2));
