"use strict";
// Quesiton No. 63
// Making type Alias
// writing a program to defice circle
const circle = {
    kind: "circle",
    radius: 10,
};
// writing a program to define a rectangle
const rectangle = {
    kind: "rectangle",
    height: 10,
    width: 20,
};
// showing results
console.log(circle);
console.log(rectangle);
